# Development / test dependencies. Install with:
#   pip install -r requirements-dev.txt
# (pulls in the runtime deps too). The production image installs only
# requirements.txt, so these never ship.
-r requirements.txt

pytest>=8.3,<9
httpx>=0.27,<1
