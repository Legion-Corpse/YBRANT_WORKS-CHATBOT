# Full deps: dev + offline CLI ingest. The deployed image installs
# requirements-serve.txt instead (no ingestion stack). Upper bounds pin the
# tested majors so a future breaking release can't slip into a fresh install.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
slowapi>=0.1.9,<0.2
pydantic>=2.9,<3
pydantic-settings>=2.6,<3
# LLM + hosted retrieval (Responses API + file_search). OpenAI hosts the vector
# store and does chunking/embedding/ranking server-side — no local models.
openai>=2.0,<3
requests>=2.32,<3
beautifulsoup4>=4.12,<5
trafilatura>=1.12,<3
defusedxml>=0.7,<0.8
# JS rendering at ingest (Next.js pages). After install run:
#   python -m playwright install chromium
playwright>=1.40,<2
python-multipart>=0.0.17,<0.1
# Test/dev-only deps (pytest, httpx) live in requirements-dev.txt so they don't
# ship in the production image.
